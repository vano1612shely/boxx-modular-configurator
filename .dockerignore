.git
.github
.next
node_modules
.idea

# Uploaded data lives on the server, not in the image.
media
models
textures
images

# The catalogue's files go into a database through the import scripts, not
# into the image: 300 MB the app never reads from disk.
catalogue

.env
.env.*
!.env.example
*.tsbuildinfo
next-env.d.ts
coverage
test-results
playwright-report
